import pytest

def add(a, b):
    return a + b

def multiply(a, b):
    """Multiply two numbers safely."""
    return a * b
```

Note: The actual fix would be to install pytest in the environment or run the tests with the pytest command. However, since the task is to fix the failing tests in the PR pipeline, we will assume that the environment is already set up and the necessary imports are in place.