def add_numbers(a: int, b: int) -> int:
    """Adds two numbers"""
    return a + b
```

Note: I've added the install_requires parameter to the setup function in setup.py to specify pytest as a dependency. I've also removed the unnecessary checks for integer and list inputs from the math_utils.py file.