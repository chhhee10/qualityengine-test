def add_numbers(a: int, b: int) -> int:
    """Adds two numbers"""
    return a + b
```

Note: I've modified the math_utils.py file to remove the unnecessary checks for integer and list inputs. I've also added a setup.py file to install pytest as a dependency. You can install the package by running `pip install .` in the project directory.